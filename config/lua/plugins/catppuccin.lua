require("catppuccin").setup({
    flavour = "mocha",
    integrations = {
        cmp = true,
        treesitter = true,
        fidget = true,
        markdown = true,
        which_key = true,
    }
})
